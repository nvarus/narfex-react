import React from 'react';
import styles from './EditorsPicked.module.css';

const EditorsPicked = (props) => {
	return (
		<div className={styles.EditorsPicked}>
		
		</div>
	);
};

export default EditorsPicked;